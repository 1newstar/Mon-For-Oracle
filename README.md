It is frame that could monitor Oracle Database by Perl DBI and SSH ,not a agent 
这是一个perl的监控框架，用perl DBI和SSH，不需要在被监控的主机上安装插件，mon.pl是主程序。
cfg,sql.xml用来配置监控的SQL，要求返回值：监控项,值
exam1.xml是一个例子，是对exam1这个主机及在这个上面的oracle数据库的监控项与阀值配置

